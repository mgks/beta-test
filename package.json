{
  "name": "docmd",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "docmd dev",
    "prebuild": "node scripts/fetch-sponsors.js",
    "build": "docmd build && node scripts/rewrite-links.js",
    "preview": "npx serve site"
  },
  "dependencies": {
    "@docmd/core": "^0.8.15"
  }
}